# java-calculator-precourse
입력한 문자열에서 숫자를 추출하여 더하는 계산기

### 구현할 기능 목록
- 문자열을 입력받는다.
- 입력받은 문자열에서 기본 구분자(쉼표, 콜론)를 기준으로 분리한 숫자를 추출한다.
- 기본 구분자 외에도 "//"와 "\n" 사이에 위치하는 커스텀 구분자로도 숫자를 분리한다.
- 각 숫자의 합을 반환한다.
- 양수가 아닐 경우 IllegalArgumentException을 발생시킨 후 애플리케이션을 종료한다.